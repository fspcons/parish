services:
  datastore:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    command: >
      gcloud beta emulators datastore start
      --project=parish-local
      --host-port=0.0.0.0:8081
      --no-store-on-disk
    ports:
      - "8081:8081"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
